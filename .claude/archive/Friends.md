

Now we need a kind of way for... users to kind of 'share' there "friend contact card" with anyone else, which should just print out all the config information another
user, using slackers, will need to copy, and manually insert into their local friend db/config file, or otherwise into a prompt in the application when it's running.

Let's try to come up with a way to do that.
First, we should have a better screen dedicated to managing friends, and personal connecton info, with the ability to 'share' my current 'contact info' with another user.

In the Settings window, add a new option for 'Friends Config', and when entering this option, show a new pane to manage all the friend and connection options and actions here.
In this pane, show these options:
- Friends List: Entering this option should show another pane with the list of personal friends that are currently configured for the local user, and basic info about them, ie. whether they are online, connection is secured (or just regular e2e), the date the friend was added to the friends list, maybe the 'last online' time, if they are not currently online. We should store these extra friend data and info locally, when friends are created or connected together, so ensure the friend system adds those, and can display them. Cache whatever data is possible, but ensure the friend polling and 'checks' between connected 'slackers' tui users will work seamlessly during app start, and occassionally check to poll and see if friends are online or not (or otherwise each
friend that is connected directly should automatically send a 'disconnected' event, through their tunnel or socket, whenever they drop off, if possible). Then other slackers clients connected to them should get the message, and can update their local config and friend statuses accordingly.
This friends list should also allow for editing for individual friend info, for each person, as a new "Friend Profile Edit Page" page.
The edit page should include all basic friend info, connection details (date added, online activity, etc), and also show a list of all editable friend connection/info fields below it, that the current user can see, and edit to change if they want (similar to the Setting page options and fields).
Make this component, and when entering on a user in the Friend Config > Friends list, it should open that friend's profile information in this component to edit or change any of the locally configured 'friend profile info' for that current friend. There should be options to save or cancel the changes here.

We don't need to really ping or keep connections "open", all the time, for friends that are online, but that we aren't talking to. That would cause too many open, unecessary connetions, for all friends connected using slackers. Instead, still have the background friends system and connections try to occassionally 'poll' for each friend, to see if they are online or what their status is. It should do this for the most popular, or most recently active friends, if possible, in an efficient fashion to 'ping' their
configured friend endpoints, for example, and if it receives a response it can consider them 'online'. Otherwise, don't make the "permanent" or open connection to those friends until the users actually start chatting together. When two friends initiate a chat, a direct connection should be initiated in the background, with the existing code that handles that, separately from slack, and then a status should be shown in their direct message window, with a special border or styling to indicate they are direcetly
connected, and it is private and secure. It should be able to do all of that outside of anything dealing with slack itself, and be able to rely only on the saved friends endpoint connection parameters. If these endpoints or the secure communication relies on the slack token or public key, to do the encryption, then we need to instead change this encryption key to be stored LOCALLY, for each of the slack friends that are connected. If possible, we should generate this key, when friends 'connect', and store it in
the locally saved friend connection info, and that should be used as the encyrption key between those two parties. It can be regenerated later by us, and sent to those two users. We could still possibly leave the slack encryption public key, in case users wants to do the friend connections through slack only, and not outside, in which case their should be a flag on which public key to use (ie. this public one, or the privately generated one). The private key can also utilize slack's public key, if it wants,
during the key generated, so that the private keys have an extra layer of hidden security/encryption, but maybe that is not necessary. Fix any of these design issues as needed to achieve the private key exchanges, and try to keep that entire flow outside of slack, if possible (or support both, optionally). These 'ping' and friend list information check actions can share the same code the channels list sidebar does, or uses, to obtain it's 'connected' or online statuses for the existing list of Friends, shown there, if possible.

Once we have worked out the logic for friend connections and communcations, then let's finish the Friends Config panel:
- add an option to "Edit My Info" and show the profile info edit screen, like we are editing another friend's info, but make it for the current user, so the current user can change their information here, if they want, for all connection and profile parameters (ie. whatever will be sent with the 'Connection Info' or 'Friend Profile' data, during friend connections. This edit panel should also include the ability for the user to set their own 'Name' (what is shown to others in this friends feature network, during connections, etc), and also their email address here. The email can be used as a uniquely identifying piece of data, if it is given, so the system can later validate if a friend config is unique, or conflicting or not.

- also in this panel, add another option for 'Share My Info...', and when entering this option, print out the json string of the current user's shared/secret endpoint info,
that they can copy and paste, and give to other users to manually add to their 'friends' configurations. This should include all necessary profile, encryption, and connection
(endpoint+port) information, as well as any other necessary info for the private/background friend connection.

- also add another option to this menu for 'Add a Friend...', and when entered, show a screen to fill in all of the fields for connecting a friend (ie. name, endpoint, port,
everything that is necessary). Also show an option to 'manually enter' the json, and when entered, show a textarea input screen to paste in the previous json for adding a
friend manually. The user can confirm, and go back to the Friend entry/info screen, and the system should try to use that JSON to fill in the field entries on this first
entry page. Then from here, the user should be able to confirm the entry from this "Friend Profile Edit Page", and once they enter 'accept' or add the friend, it should try
to verify that friend doesn't exist already (ie. same endpoint + user identifier, or something), and show an error or warning if so, and otherwise add that friend connection
into to the internal list of friends.

- also add an option in this Friend Config panel, to 'Export Friends List', and when entered, it should export all of the friend profile information from the local setup as a json array of friends. The export action should
generate the entire array of json, and download it into the user's currently configured Downloads folder, and show a message at the bottom when it completes that, and where it is on the user's system. The export and import system can choose to validate or organize the exports based on unique fields, such as the generated slacer guid's for the users, when they install it, or otherwise a unique slack user id, or their configured email address, if given. The system should be kind of smart, and check all possible "uniqueness" properties, depending on which are provider, and include them. The import mechanism should go through and check on these fields individually, depending on what is provided (see next).

- there should also similarly be an option here to "Import Friends List", when entered it should show a pane to enter a path to the filename to import, and also hit enter on it to open the file select browser pane we made, where the user can select the json file to import. Once a file is selected, the screen should show the options to accept/import the file, or cancel/go back. There should also be a checkmark option in this panel, shown when a file is loaded, to "Overwrite Conflicts", or not, with a message that explains enabling the option will force-override any existing conflicting friend configurations (which match their name/identifier, same endpoint, etc). If this option is enabled, the imported friend file should merge with the existing friends list, and override any conflicting or "same" entries (ie. same endpoint/ports, same id, same email or something, etc). If the option is not enabled, the importing conflicted information should be skipped, but still import friend profiles from it that don't conflict. The import should check on the possible "unique" fields, mentioned above, and flag a conflict if any of them do conflict with existing entries. For whatever entries will be 'overwritten', it should match on whatever unique field did match, and replace those field values with the imported values. Otherwise, it should skip those entries entirely if they were flagged as 'conflicting', from any fields, if the import chose to 'skip conflicts'.

We can also move or duplicate the existing 'Settings' > Secure Mode, and 'P2P port' options to this info edit screens, maybe... for the current user. ie. when they enter 'Edit My Info', maybe we can also expose the endpoint and p2p port options here, that will be used by the app and sent to the other users for connecting to. I'm not sure if that should be different from the existing main Settings, for the user, for each individual friend... but if it is supposed to be the same for the current user, then duplicate the settings here, and ensure changing it in both areas (ie. the main settings page, or the Friend Config > Edit My Info) page... they should still both change it both in the settings and friend config info, no matter where it was saved or changed from, as a convenience). Modify those editable endpoint/port settings as needed to make them editable in the proper way, for the current user's friend/connection profile.

Maybe it would also be beneficial for the slackers program to generate a unique identifier, for all slackers users, which could be stored in their connection info, and traded around and used for a more strict 1:1 relationship between friends. I'm not sure if that is necessary, but if you think we can determine that a user is "unique" simply from their other identifying info (ie. hosetname, with port, and something else), we could do that, but I'm not sure we'll have enough info in the basic case of friends connecting outside of slack, with just some basic connection info. In that case, we could help it by generated a unique id for any slackers user, each time they install the slackers app, and store it locally in their session data, then send that will all 'friend' connection info, so slackers users have this kind of fallback identifier they can validate against for uniqueness. Maybe that would be worthwhile, but do what you can to ensure uniqueness somehow, easily and automatically, for users and friends. We can also fallback, or should try to use their other unique profile information, if they provided it, such as the email address, or other fields, if they can determine uniqueness, in these cases.

Go ahead and try to ensure that plan is complete, and fix any missing design decisions, or gotchas, but make it seamless to manage and add all friend information, edit their profiles, and my own, and share all of the config information easily between friends, so they can try to connect as easily as possible.

Also add some small help messages in ie. the Friend Config screen, and its sub-screens (such as Share my Info, or Edit my Info), to explain basically how it works, what the parameters/endpoints should be, and how the users should connect, or what else they need to do outside of this app (ie. setup port/firewall rules, and to refer to the help if needed). Maybe we can also make a sub-help page for viewing this info. Add a new command 'slackers help friends' that prints out the entire how to in order for two friends to connect privately, outside of slack. It should explain exactly what they have to do in their firewalls/routers, to forward ports, open them on their computer (show the variations for each type of platform dynamically when the command runs, by detecting the user's platform and show only those commands or syntax for that platform). Also explain extended config options here, and what they can do to modify the basic functionality/configurations. Then, also summarize this feature fully, and add a section for how the "Friends" feature works, to connect securely and privately outside of slack's system itself, somewhere towards the bottom of the main readme area (before development sections), in a brief collapsable section. Make note of the prominent features and extensible options. You should also update the How_It_Works.md document with a more extensive overview of how the feature works, in there as well.


--------------------------------------------------------------------------------

Fix these issues:

- In 'react mode', when I have a message selected, and hit Enter on it, the emoji picker is not popping up. Nothing happens. Can you get the emoji picker to pop up in the page, in the middle of it, without clearing the background? Just have it overlay, with a blank border around, if possible.
- When hitting 'Ctrl+j' in the message history, it goes into react mode, but can you make this happen when focused on any panel? If the user hits ctrl+j (or whatever the configured emoji picker shortcut is), then the focus should go to the chat history window, and enter react mode immediately.
- Also, are you sure we can't get mouse mode to detect when a messasge in the chat history is clicked (right click is ideal, but left click is fine), and show an options menu there on the screen? Can we clone the existing screen output, then render the options or emoji panel on top, at that position, and then display that temporary renering while it's open, and replace it after with the refreshed chat history when it goes away, or something like that? Anyway, try to get the options panel to show at that position, when right clicking a message, and show the options for 'React', and also 'Reply'. When clicking React, show the emoji picker reaction panel for that message, in that position, and then when an emoji is selected, have it use the underlying means to send the reaction to the other party.
- Message replies (new feature): If the user clicks the 'Reply' option, in the message options menu, have the text focus go back to the chat input, and add a new special syntax to signal a message reply, at the top, ie.: [REPLY:<message-id-or-reference>] "Brief message sample..."\n, or something like that. This is similar to the file insertions syntax.
Then, when the user hits enter for a message, the system should parse the input, and detect the [REPLY:id] format in the message, at the top, and understand it is a reply to that message. Then, it should detect if the user is in a direct/private "Friends" chat, or otherwise the normal slack chat, and try to reply to that message accordingly. If it's the reguar slack connection, it should use the slack api to respond and reply within that message's context, however that has to happen. If instead it is a private "Friends" chat, the reply should use a new underlying command or action, to signal the message is a reply to the given one, and the underlying message handler for the direct connection should parse out the [REPLY:id] syntax, find that given message in the chat history, and reply to it there, with the attached message after the detected reply syntax.

Then, in the chat history UI, implement a new means to show an "X Replies" message, after any chat message entry which might have replies for it. It should show this message on the left, under that message, and also have any of the previously creates "Reactions" on the right side, in this same row. The reactions and reply indicators should show on the same row, reply count/indicator on the left, and the reactions summary on the right.
The private chat history manager should be able to store replies for messages within their local saved JSON, as a child tree of entries, and be smart enough to manage and find them there, to add, edit, or otherwise react to those replies, just the same as normal messages.

Then, we need a way for a user to see the replies. This should be done in multiple ways, but I'd like to give the users another option:
Add a new setting in the Settings panel, and default settings, for showing replies "inline", ie. "Reply Format": "inline", or "inside", and explain that the "inline" method will show replies directly in the chat, tabbed over a bit under the current message. Otherwise, the "inside" option will just show the count of replies, under the messages, and then the user will have to go into message or reply mode, and 'enter' on that 'X Replies' status label, in order to go "inside" of that message chain. When that happens, the chat history should clear, and show that message chain only, but retain the context window and all. It should show a custom title at the top signifying which message chain it is in (ie. who initiated it, what message it started from, and it should show the entire initial message the replies are attached to, at the top, perhaps in a scrollable area... where the user can use the arrows to go up to select it, or scroll it with the mouse to read it more, if it was a large message). Have this "inside" screen show the message chain, and have any entered or sent messages, in this view, be automatically sent as a reply to the original message, at the top, further adding it to the current chat history, and rendered output.
Otherwise, if it's an 'inline' view, the replies should render below that current message, always shown fully, but tabbed to the right a bit (ie. 4 spaces), to indicate they are 'within' that parent message, as replies. Then just render the child reply entries out here, in a list within the same history view, and then continue with the other messages, outside of this parent, below it, as it normally does. In order to reply to a message, in this mode, the user will just right click on the parent message again, and click reply, and it will do the same work of adding the [REPLY:id] entry in to the text input, focusing after it, and letting the user enter a message, and the system should pick that up, send the reply through whichever appropriate means, and then append that reply to that message id's existing list of 'reply children', in the history, and have the history view update accordingly thereafter.

Go ahead and implement all of that, fixing any holes or issues as you see fit. If we can't get a right click menu to show, we should display it through some other means, but try to make it non-invasive (ie. don't replace the whole screen, just show some basic options to react, or reply somewhere, and then perform those actions accordingly).

* option to render repliess "inline, tabbed", or have to enter into the replies...


--------------------------------------------------------------------------------

Add global 'friends' list:
I'd like to now add a feature to manage a global 'Friends' list, for users of 'slackers' to befriend each other.
If a user requests to be added as a 'Friend', it should imply they are already talking with an e2e or p2p connection (ie. they both have slackers installed, and should be online, using it).
Befriending a user should ask the current user to confirm (in a popup window, or in the status bar as a temporay input there, escape to cancel), and then initiate the request.
Once a user initiates a request, the other person's slackers client should pick it up, in the background (ie. through the p2p tunnel, or otherwise however it needs to), and the other user should be prompted to accept the friendship in a popup window, showing the users name.
Once a friendship is established, a record of the friendship should be stored in both users local settings, and restored on app reload.
If a user has any friends defined, they should show as a new "channel group" on the left sidebar, but these be completely outside of slack, so they shouldn't be expected to be fetched or managed by slack, but instead just read from the local config, and merged into the current channels. Ideally they would work with existing system, as regular channels, in all aspects, but just be marked as a sort of 'private' or 'friend' channel.
This signals that communication betweent those two parties should always be done outside of the slack normal channels, and instead through their background 'friend' tunnel endpoints or methods of communication.
Render the users 'friends' just the same, in the channels sidebar, in a collapsable "Friends" section.

To initiate the befriending, the user should be selected from the channels sidebar (ie. direct messages person), or otherwise there should be a shortcut to try to before the current user they are talking to. It should try to validate the the befriend attempt is happening for a single user, ie. direct message. Also, it should check that both users are using slackers, and try to obtain and use their connection info for the request, or at least try to initiate that process for them, and automate it as much as possible. If the other user is not using slackers, a message should be sent instead to them, through the slack api, inviting them to try slacker tui, with a link to the github, and that they'd like to befriend them there to chat privately. Then show a warning or info message to the initiating user mentioning the other user is not using slackers, but has been invited to slackers.

Also try to enable or change the system, when implementing this feature, to first try to load the app in a blank state (ie. the panes with no data).
Then, try to load the friends list, as the first channels in the left sidebar.
From, there, it should check if the user is actually part of a configured workspace, or if a workspace is activer, and then try to load the user's workspace data for it, like it currently does, and populate or update the views..
All previous state should be loaded after a workspace is loaded, except the friends list should be rendered first. If no workspace or session data is found, it can still try to load the previous state, but shouldn't error except for basic warnings, if it tries to load a workspace but can't for some reason, and still let the user operate with their registered friends, and talk to them through private connections.

Friend/private chats should operate like regular chats, showing the chat input, history, and all, but those processes, in the case of 'friend' or 'p2p' chats, should be managed by our own internal systems, and populate those panes with our internal data, and our means of communication (ie. through the tunnel or howevever).
If a workspace cannot load, but a friend is 'online', the app should detect this.
In the friends list, show a special indicator for 'online' friends, vs. offline friends (ie. grey out the offline friends, and show online friends in white or something). The system should 'ping' each friend when it starts, to see if they are online.
When opening a friend chat channel, it should try to establish the backend websocker or p2p connection with, privately, through the registered or saved friend 'p2p config' endpoint and port information. Any errors should be displayed in the ui and the debug logs.

In this way, the user should be able to open slackers, even if not configured to a workspace, and still try to establish private connections, and chat with their Friend from their friends list.


- When a user is selected in the side panel, or otheriw
- save people as friends: stores their network, user inefo, and slackers p2p connection info.
- app can start, show 'friends' list in sidebar (online/offline indicators)
	- chatting with friend here opens private outside connection
- app should be able to load without a workspace, and just try to load the friends list separately from the current workspace
	- app starts: loads blank state, loads friends, tries to load previous workspace...

