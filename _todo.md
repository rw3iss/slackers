Can you add these features?

- add an ability to search and goto a channel quickly: Make a shortcut to enter a search panel, where I can start typing name, and a results list shows to filter the existing channel by the name, hitting enter chooses the first result, or I can use arrow up/down to move to another result and select another channel to enter from that menu, and go to it.

- add the ability to hide a selected channel by another shortcut, and then a shortcut to view the hidden channels, and select ones to unhide. The hidden channel data should store in the user's local app settings, and restored on app restart.

- add the ability to give names or aliases to the 'group chats', which currently just show the abbreviated and hyphenated names of each group member. Allow the user to 'rename' a group, based on its internal group id, and save the group name with the local app settings,restoring it upon app reload, so it's easier for users to view the group chats by some alias, instead of the abbreviated initials.


Ensure the readme has a brief list of these features, and all of the other ones that are noteworthy.

--------------------------------------------------------------------------------

We need the left sidebar to scroll up if the cursor goes up above the list. It is currently cut off at the top because their are to many channels. If I press the keyboard up or down arrow in the list, and the current channel is above or below the screen, the screen should page down or up, adjusting itself to keep the selected channel in view.

Also, for the hidden channel feature, ensure there is a toggle shortcut to show the hidden channels in the main view, with the rest of them, if the user wants. ie. it can be something like 'Ctrl+Alt+H' or similar, and then toggle it back to hide them when pressed again.

I'd also like to add a channel 'Sort by' setting, in the settings panel for the app.
This should offer a means to sort the list of channels, which could be whatever properties we have available (find them all), ie: alphabetical/name, date created, maybe last updated/most recent messages, private vs. public, etc... I don't know what else.
Also offer an option to change the sorting direction, as a separate setting.
These settings should persist to the local app settings, and reload on app restoration, just as well as all settings should.

--------------------------------------------------------------------------------
